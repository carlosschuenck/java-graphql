type UserDto {
    id: ID!
    name: String!
    age: Int!
    address: AddressDto
    orders: [OrderDto]
}

input UserInput {
    name: String!
    age: Int!
    address: AddressInput
}

type Query {
    users: [UserDto]!
}

type Mutation {
    newUser(user: UserInput): UserDto!
}